# タスク

> `/spec` フェーズ 3 の成果物。`/task` で上から順に消化する。
> 書式: `- [ ] タスク名 — 完了条件`
> 対象機能: 今夜のスケジュール立案(requirements.md / design.md 準拠)

- [x] T1 型定義と時刻ユーティリティ — `lib/time.ts` のユニットテストが通る(2026-07-08 完了)
  - `types.ts`(RoutineTask / PlanItem / TonightPlan / Settings)
  - 夜通算分のパース(`"24:30"`→1470、`"0:30"`→1470 正規化)・フォーマット(1470→`"24:30"`)・現在時刻→夜通算分・nightKey 判定(朝 4 時境界)
  - テスト観点: 0 時越え、3:59/4:00 境界、不正入力

- [x] T2 スケジューラ — `lib/scheduler.ts` のユニットテストが通る(requirements.md の具体例を再現)(2026-07-08 完了)
  - `buildSchedule()`: 固定時刻タスクの配置 → 可変タスクの隙間詰め(順序不変)→ gaps / 自由時間 / 超過リスト / 警告(固定同士の重なり・固定の時刻超過・就寝オーバー)
  - テスト観点: 具体例(21:10 帰宅ケース)、固定なし、全部超過、固定が過去、空リスト

- [x] T3 永続化層 — `lib/storage.ts` のユニットテストが通る(2026-07-08 完了)
  - `sp.v1.*` キーの load / save。JSON 破損・型不一致時は初期値へフォールバック(クラッシュしない)

- [x] T4 アプリ骨格と設定タブ — 3 タブが切り替わり、デフォルト就寝時刻の変更がリロード後も保持される(2026-07-08 完了)
  - 下部タブバー(今夜 / ルーチン / 設定)、モバイルファーストの基本スタイル
  - 設定タブ: デフォルト就寝時刻(24:30 形式入力+バリデーション)、データ全消去(確認 2 段階)

- [x] T5 ルーチンタブ — ルーチンの追加・編集・削除・並べ替えができ、リロード後も保持される(2026-07-08 完了)
  - 一覧(名前 / 所要分 / 固定時刻バッジ / ▲▼ / 編集 / 削除)、削除は確認ダイアログ
  - 追加・編集フォーム(名前・所要分・任意の固定開始時刻)

- [x] T6 プラン作成ビュー — タスクを選んで「スケジュールを作成」するとタイムラインと自由時間が表示される(2026-07-08 完了)
  - ルーチン全件が選択済みで初期表示(`included`)、当日タスク追加、▲▼並べ替え、就寝時刻の当日変更
  - 作成後の実行ビューはこの時点ではタイムライン表示のみで可(完了チェックは T7)

- [x] T7 実行ビュー — 完了チェックで残りが現在時刻から引き直され、超過警告と全完了のご褒美画面が動く(2026-07-08 完了)
  - NOW カード(今やるタスク+終了予定+完了ボタン)、完了チェック解除、途中のタスク追加・外す(included=false)と復帰
  - 超過警告バナー+超過タスクの赤表示、「おつかれさま 🎉 自由時間 ◯分」画面
  - `useNow`(30 秒間隔)で時間経過による引き直し

- [ ] T8 夜境界リセットと仕上げ — 翌夜にプランが自動リセットされ、スマホ実機で一通りの操作を確認済み
  - nightKey 不一致で前夜プランを破棄してプラン作成ビューへ(F5)
  - favicon・タイトル調整、README に運用注意(PC の IP 固定 = DHCP 予約推奨、サイトデータ消去でデータが消える)を追記
  - Android 実機で: ルーチン登録 → プラン作成 → 完了チェック → ご褒美画面 の通し確認
